---
description: Clinical Co-Pilot (Gauntlet) project conventions and deploy facts
globs:
alwaysApply: true
---

# Clinical Co-Pilot

- Case study north star: `docs/directions.md`. Demo data only; treat LLM (via OpenRouter) as BAA-covered / no training.
- Stack: **hybrid** session-proxy OpenEMR gateway + **LangGraph** sidecar (single worker, same 2 GB host); **OpenRouter** pin `anthropic/claude-haiku-4.5`; **LangSmith** (redacted). LangGraph ≠ LangSmith.
- Auth: session-proxy (no browser cookies to sidecar); tool-layer **pid** fail-closed; patient picker if unbound; SMART later.
- Chart: PHP **services via gateway** (FHIR primary = phase 2). Research: **openFDA → DailyMed** (sidecar-only; PRD 05); no PHI; dosing only from retrieved sources; **conflict UX deferred** for MVP; off-chart named drug OK + not-on-list honesty.
- Verify: structured claims, cite-or-silence; keep `source_type`; **hybrid SSE** (progress early; clinical after verify). State: open-tab transcript until closed.
- Jobs: UC-1 brief · UC-2 labs · UC-3 med decision-support (read-only chart). UX: Ask Co-Pilot tab; empty chat; citation popups; concise; omit > guess.
- OpenEMR-side code in `/src` (BaseService when it fits; Chart/Schedule isolatable). Canonical plan: `ARCHITECTURE.md` (+ `docs/architecture-overview.md`). PRDs: `docs/PRDs/`.
- Ambiguity / shortcuts / cut order / physician UX / escalation: `docs/ai-decision-guide.md` (under ARCHITECTURE). Engineering guesses OK; clinical claims = verified+cited, labeled unverified, or refuse. Fake only non-spine work; note debt in Memory Bank. Keep shipping spine; batch DO fixes before interview. ~90s physician budget (post-cache); link every verified claim; clinical-ish progress (`Pulling labs…`).
- Public deploy: https://142.93.255.212/ (`admin`/`pass`). SSH: `ssh -i ~/.ssh/id_ed25519_openemr root@142.93.255.212`. Stack: `/opt/openemr`.
- Git: `origin` = `eweinhaus/openemr-base-clean`, `upstream` = `Gauntlet-HQ/openemr-base-clean`.
- Next: optional PRD 06 Source click-path smoke; then PRD 07 LangSmith thin. PRD 06 on DO (`58eb115`). Read `memory-bank/` at session start; use decision guide when stuck.
- Builder: mid-level SWE; PRD/plan carries mental models; don’t assume FHIR/SMART/SSE/OpenEMR fluency. Interview MVP > rubric / production polish.
